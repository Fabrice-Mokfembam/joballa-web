import { SignUpProfileRedirect } from "@/components/auth/sign-up-profile-redirect";

/** Document upload moved to worker settings; keep URL for bookmarks. */
export default function SignUpProfilePage() {
  return <SignUpProfileRedirect />;
}
