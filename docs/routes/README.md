# API route reference (Joballa Web repo)

The **authoritative REST contract** for the Nest API consumed by this app is:

- **[`../BACKEND_AUTH_REPLY.md`](../BACKEND_AUTH_REPLY.md)** — §8 (`/auth/*`), §9 (profile `*/me` routes), JWT/cookies, errors, operational limits.

The backend team may maintain parallel markdown under their own repo (`docs/routes/02-auth.md`, etc.). This **joballa-web** tree does not duplicate those files; use **`BACKEND_AUTH_REPLY.md`** here.
